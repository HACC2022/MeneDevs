const getCreds = document.getElementById("login");

getCreds.addEventListener("click", function()
{
    const email = document.getElementById("email");
    const pass = document.getElementById("pass");

    if(email == null || pass == null)
    {
        return;
    }
})